# Handle Microsoft Edge from the PowerShell command line

## Start Edge

Just start Edge: \
`start msedge`

The command `start` is short for the `Start-Process` cmdlet.

Start Edge with a specific site: \
`start edge www.microsoft.com`